import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arij.front',
  appName: 'mini-project-front',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
